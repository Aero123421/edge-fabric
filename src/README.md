# src

このディレクトリは Python reference track です。

binary on-air の正本ではありません。主用途は `legacy compact` の regression 比較、fixture replay、contract model の補助検証です。

- `edge_fabric/contracts`
  参照用 contract model
- `edge_fabric/host`
  reference `Site Router` / `Host Agent`
- `edge_fabric/sdk`
  reference client
